
// This component has been replaced by the new PaymentSyncProvider system
// It is kept for backward compatibility but no longer performs any operations

const PaymentSync = () => {
  // Component is no longer needed - payment sync is now handled by PaymentSyncProvider
  return null;
};

export default PaymentSync;
