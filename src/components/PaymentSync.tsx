
// This component is deprecated - payment sync is now handled by usePaymentSync hook
// Keeping for backwards compatibility but it does nothing
const PaymentSync = () => {
  return null;
};

export default PaymentSync;
